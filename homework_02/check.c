#include <stdlib.h>
#include <stdio.h>

#define M 10000000

int arr[M] = {0, 0, 0, 0};

int main() {
	const int N = 10;
	int a[N];
	for (int i = 0; i < N; ++i) {
		a[i] = i * i;
	}

	for (int i = 0; i < N; ++i) {
		printf("%d ", i[a]);
	}
}
